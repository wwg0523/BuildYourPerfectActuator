server {
    listen 5005;
    server_name _;

    root /app/build;
    index index.html index.htm;
    
    # 요청 본문 크기 제한
    client_max_body_size 10M;

    # 모든 정적 파일 (images, videos, css, js, fonts 등)
    location ~* ^/(assets|public|videos)/ {
        root /app/build;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000, immutable";
        add_header Access-Control-Allow-Origin "*";
        access_log off;
    }

    # 정적 파일 확장자 캐시 (js, css, images, fonts, videos 등)
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|mp4|webm|ogg|mov|avi)$ {
        root /app/build;
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
        access_log off;
    }

    # /api 경로를 백엔드로 프록시
    location /api/ {
        proxy_pass http://actuator-back:4004/api/;
        proxy_http_version 1.1;
        
        # 프록시 헤더
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        
        # Content-Type 헤더 유지
        proxy_set_header Content-Type $content_type;
        
        # WebSocket 지원
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;
        
        # 요청/응답 버퍼링 비활성화 (실시간 스트리밍)
        proxy_buffering off;
        proxy_request_buffering off;
        
        # 타임아웃 설정
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # SPA 라우팅: 모든 요청을 index.html로
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 에러 페이지 설정
    error_page 404 /index.html;
}
