# Frontend environment variables example
# NOTE: API calls are now using relative paths (/api)
# which are handled by Nginx reverse proxy in production

# Encryption key used by the front-end to encrypt local user info before submitting.
# Must be kept secret in production (use a sufficiently long random string).
REACT_APP_ENCRYPTION_KEY=your-32+byte-long-secret-key

REACT_APP_ANALYTICS_PASSWORD=your-analytics-password

# Google OAuth Client ID
# Get this from Google Cloud Console
REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id-here