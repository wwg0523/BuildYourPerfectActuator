# Backend environment variables example
# NODE_ENV=production enables HTTPS enforcement middleware in the app.
NODE_ENV=production

# Public-facing URL of the frontend (used for CORS in production)
FRONTEND_URL=https://your-frontend.example.com

# Port the Node server listens on (internal)
PORT=4000

# Admin password for analytics endpoint
ANALYTICS_PASSWORD=super-secret-analytics-password

# Email account used to send mails (Gmail service used in transporter)
APP_EMAIL=your-email@gmail.com
APP_PASS=your-email-password-or-app-password
DB_USER=postgres
DB_HOST=localhost
DB_NAME=actuator_game
DB_PASS=super-secret-key
DB_PORT=5432
PORT=4000

APP_EMAIL=your-email-here
APP_PASS=your-app-password-here