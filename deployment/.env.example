# Backend Configuration
DB_HOST=actuator-db
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=1209
DB_NAME=actuator_game
API_PORT=4004

# Frontend Configuration (API calls use relative paths /api via Nginx proxy)
PORT=5005

# Email Configuration (Gmail SMTP)
APP_EMAIL=whwlsgh0523@gmail.com
APP_PASS=invb xoqc sqtx qeyw

# Environment
NODE_ENV=production

# GitHub Webhook Configuration (자동 배포)
# Webhook 서버 포트
WEBHOOK_PORT=3000

# GitHub Webhook Secret
# GitHub Repository > Settings > Webhooks에서 생성한 Secret 값을 입력하세요
WEBHOOK_SECRET=your-webhook-secret-here

# 업데이트 스크립트 경로
UPDATE_SCRIPT=/volume1/actuator-minigame/update-docker.sh
