# Backend Configuration
DB_HOST=actuator-db
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=1209
DB_NAME=actuator_game
API_PORT=4004

# Frontend Configuration (API calls use relative paths /minigame/api via Nginx proxy)
PORT=5005

# Email Configuration (POP3/SMTP - Mailplug)
APP_EMAIL=sales@lebot.co.kr
APP_PASS=yZO-)B2hp[q&7S(M8WU.

# Server Configuration
# Docker 내부 네트워크에서 컨테이너명으로 접근
# 프론트엔드 → 백엔드: actuator-back
# 백엔드 → DB: actuator-db
SERVER_HOST=actuator-back

# Environment
NODE_ENV=production

# GitHub Webhook Configuration (자동 배포)
# Webhook 서버 포트
WEBHOOK_PORT=3000

# GitHub Webhook Secret
# GitHub Repository > Settings > Webhooks에서 생성한 Secret 값을 입력하세요
WEBHOOK_SECRET=your-webhook-secret-here

# 업데이트 스크립트 경로
UPDATE_SCRIPT=/volume1/actuator-minigame/update-docker.sh
