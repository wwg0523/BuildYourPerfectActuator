# ════════════════════════════════════════════════════════════════════
# 환경 설정 템플릿 (복사 후 실제 값으로 수정하세요)
# 
# 개발 환경: cp .env.example .env.dev
# 프로덕션: cp .env.example .env
# ════════════════════════════════════════════════════════════════════

# ──── 데이터베이스 설정 ────
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_NAME=actuator_game

# ──── SMTP/이메일 설정 (POP3/SMTP) ────
# Mailplug 서버 설정
SMTP_HOST=smtp.mailplug.co.kr
SMTP_PORT=465

# 이메일 계정
APP_EMAIL=your_email@mailplug.co.kr
APP_PASS=your_email_password_here

# ──── 분석 기능 비밀번호 ────
# /api/analytics 엔드포인트에 접근할 때 사용
ANALYTICS_PASSWORD=your_secure_analytics_password

# ──── 암호화 키 ────
# 정확히 32바이트 길이여야 합니다 (필수!)
# 예: abcd1234efgh5678ijkl9012mnop3456
REACT_APP_ENCRYPTION_KEY=your-32byte-secret-key-1234567890

# ──── 노드 환경 ────
# 가능한 값: development, production
NODE_ENV=production
